<%= form_for @income, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="form-actions">
  	<%= f.label :amount, 'Kwota:' %>
  	<%= f.text_field 'amount' %>

  	<%= f.label :map, 'Wybierz lokalizację przypływu:' %>
  	<div id="map_canvas"></div>

  	<%= f.hidden_field 'longitude' %>
  	<%= f.hidden_field 'latitude' %>
    <%= f.submit 'Dodaj', :class => 'btn btn-primary' %>
    <%= link_to "Anuluj", incomes_path, :class => 'btn' %>
  </div>
<% end %>
