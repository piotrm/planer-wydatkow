<div class="navbar">
  <div class="navbar-inner">
    <div class="container">
      <% if current_user %>
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
      <% end %>
      <a class="brand" href="#">PLANER WYDATKÓW</a>
      <% if current_user %>
        <div class="nav-collapse">
          <ul class="nav">
            <% if current_user %>
              <li>Witaj, <%= current_user.name %>!</li>
            <% end %>
            <li><%= link_to "Wydatki", expenses_path %></li>
            <li><%= link_to "Przychody", incomes_path %></li>
            <li><%= link_to "Statystyki" %></li>
            <li><%= link_to "Ustawienia", users_path(@user) %></li>
          </ul>
          <div id="user_nav">
            <%= link_to "Wyloguj", signout_path, class: "btn btn-danger" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>