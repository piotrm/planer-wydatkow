<div class="navbar">
  <div class="navbar-inner">
    <div class="container">
      <% if current_user %>
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
      <% end %>
      <a class="brand" href="/">PLANER WYDATKÓW</a>
      <% if current_user %>
        <div class="nav-collapse">
          <ul class="nav">
            <li><%= link_to "Przepływ środków", users_path(@user) %></li>
            <li><%= link_to "Wydatki", expenses_path %></li>
            <li><%= link_to "Przychody", incomes_path %></li>
            <li><%= link_to "Statystyki" %></li>
            <li><%= link_to "Ustawienia", user_settings_path(@user) %></li>
          </ul>
          <% if current_user %>
            <div id="user_nav" class="pull-right">
              <%= link_to "Wyloguj", signout_path, class: "btn btn-danger" %>
            </div>
            <div id="user_nav" class="pull-right">
              <span class="navbar-text hello">Witaj, <%= current_user.name %>!</span>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>